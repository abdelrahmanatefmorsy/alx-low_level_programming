#include <stdio.h>

/**
 * main - entry point
 * Descriptionn: c program print with put function
 * Return always 0 (success)
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
